# ==========================================
# CONFIGURACAO PRINCIPAL
# ==========================================
# Define o profile ativo (dev por padrao, prod na VPS)
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# Nome da aplicacao
spring.application.name=agendamento